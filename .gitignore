# Project-specific files (generated per project)
config.yaml
state.json
STATE.md
context.md

# Project artifacts
artifacts/

# Graphify — local tracking only (graph.json etc. are committed)
graphify-out/cost.json
graphify-out/cache/

# Global skills live in ~/.agents/skills — local copies are not tracked
.agents/skills/
.claude/skills/

# Python / LangGraph build artifacts
eng_loop/src/eng_loop/__pycache__/
eng_loop/src/eng_loop/**/*.pyc
eng_loop/.pytest_cache/
eng_loop/*.egg-info/
eng_loop/.ruff_cache/
__pycache__/
*.pyc
.pytest_cache/
eng_loop/.cosmic-ray/
